/*
const state = {
  title: '',
  content: '',
  about: ''
}

const getters = {
  title: state => state.title,
  content: state => state.content,
  about: state => state.about
}

const mutations = {
  saveArticle(context, payload) {
    state.title = payload.title,
    state.content = payload.content,
    state.about = payload.about
  },
  clearArticle(context, payload) {
    state.title = payload.title,
    state.content = payload.content,
    state.about = payload.about
  }
}

const actions = {
  saveArticle (context, payload) {
    context.commit('saveArticle', payload)
  },
  clearArticle (context, payload) {
    context.commit('clearArticle', payload)
  }
}

export default {
  state,
  getters,
  mutations,
  actions
}

 */
